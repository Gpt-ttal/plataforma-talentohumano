# ─────────────────────────────────────────────────────────────────────────────
# Sistema Paz y Salvo — Frontend (apps/web). Copia a `.env.local`.
# Solo claves PÚBLICAS (anon key + URL). El backend valida el JWT y autoriza.
# ─────────────────────────────────────────────────────────────────────────────

# Proyecto Supabase (Settings → API). La anon key es pública.
VITE_SUPABASE_URL=https://vwcnqrdicjarkorqdrue.supabase.co
VITE_SUPABASE_ANON_KEY=

# Base del API. En dev, "/api" usa el proxy de Vite hacia http://localhost:3000.
VITE_API_URL=/api
