# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# testing
/coverage

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files
.env
.env*.local

# secrets locales (credenciales OAuth, service accounts, etc.) — NUNCA commitear
/secrets/

# config MCP local (contiene tokens/credenciales reales) — NUNCA commitear
.mcp.json

# config local de Claude (rutas absolutas de la máquina) — no portable
.claude/settings.local.json

# vercel
.vercel

# superpowers brainstorm scratch (visual companion, mockups)
.superpowers/

# graphify knowledge-graph output (regenerable con /graphify)
/graphify-out/

# playwright-mcp scratch (logs/snapshots de sesiones E2E)
/.playwright-mcp/

# typescript
*.tsbuildinfo

# monorepo build artifacts (regenerados por tsc/vite)
node_modules/
dist/
